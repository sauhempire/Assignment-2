//2. Write a program to print a given number without its last digit.
 #include<stdio.h>
    int main()
   { 
    int x;
    scanf("%d",&x);
    printf("Given number without its last digit is %d",x/10);
    return 0;
    } 
